# DynamicIsland - 代码审查与修复任务列表

## 任务 1：移除鼠标过滤逻辑
- **优先级**：P0
- **依赖**：无
- **描述**：
  - 删除 `IsPointInImguiWindow` 函数
  - 删除 `WndProc` 中添加的鼠标消息过滤代码
  - 删除 `g_imguiWindowRect` 全局变量（如果不再需要）
  - 删除相关的 `LOG_DEBUG` 日志调用
- **验收标准**：代码编译通过，运行时鼠标在 Imgui 区域内能被正确处理

## 任务 2：保留必要的窗口处理逻辑
- **优先级**：P0
- **依赖**：任务 1
- **描述**：
  - 保留 `WM_NCHITTEST` 处理（用于窗口拖拽区域）
  - 保留文件拖放 `WM_DROPFILES` 处理
  - 保留快捷键处理
- **验收标准**：拖拽区域仍然有效，其他功能不受影响

## 任务 3：测试验证
- **优先级**：P0
- **依赖**：任务 1, 2
- **描述**：
  - 测试鼠标在 Imgui 显示区域内是否正常交互
  - 测试鼠标在非显示区域是否透传给下层窗口
  - 测试文件拖放功能
  - 测试快捷键功能
- **验收标准**：AC-1, AC-2, AC-3, AC-4